<article class="post" onclick="loadPost({{this.id}})">
    <div class="article-header">
        <h2>{{this.title}}</h2>
        <p>Created on {{format_date this.created_at}}</p>
    </div>
    <div class="article-text">
        <p>{{this.post_text}}</p>
    </div>
</article>